{
  "name": "solution-compass",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:wizard": "node scripts/validate-wizard.mjs",
    "validate": "npm run validate:data && npm run validate:wizard",
    "predeploy": "vite build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.0.0",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.16.0"
  }
}
